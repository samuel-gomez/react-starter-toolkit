import { ROUTE_URL_DASHBOARD as DASHBOARD } from 'Dashboard/constants';
import { ROUTE_URL_MEMBERS as MEMBERS } from 'Members/constants';
import { ROUTE_URL_FORM as FORM } from 'Form/constants';
import { ROUTE_URL_DATEPICKER as DATEPICKER } from 'Form/Datepicker/constants';
import { ROUTE_URL_SLIDER as SLIDER } from 'Form/Slider/constants';
import { ROUTE_URL_FIELDS_CLASSICS as FIELDS_CLASSICS } from 'Form/FieldsClassics/constants';

const ROUTE_URL = {
  DASHBOARD,
  MEMBERS,
  FORM,
  DATEPICKER,
  SLIDER,
  FIELDS_CLASSICS,
};

export default ROUTE_URL;
